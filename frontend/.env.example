# Public base URL of the FastAPI backend (including the /api prefix).
# Only NEXT_PUBLIC_* variables are exposed to the browser — never put secrets here.
NEXT_PUBLIC_API_URL=http://localhost:8000/api
# Default company slug used on the registration page when none is given in the URL (?company=...)
NEXT_PUBLIC_DEFAULT_COMPANY=demo
